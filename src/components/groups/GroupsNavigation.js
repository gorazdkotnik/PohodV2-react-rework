import React from 'react';

import PageMenu from '../ui/PageMenu';

const GroupsNavigation = ({ labels, links }) => {
  return <PageMenu labels={labels} links={links} />;
};

export default GroupsNavigation;
